
%h1 Your Pragmatic Catalog
- if @count > 5 
  %strong
    You've viewed this page #{pluralize(@count,"time")}, it is time to buy
- @products.each do | product |
  .entry.hero-unit
   
    %h3 
      = product.title
   
    %p
      = image_tag(product.image_url)
      =sanitize(product.description)
   
    .price_line
      %span.price
        %strong 
          Цена:
        = number_to_currency(product.price, unit: "UA", format: "%n %u")
      
      = button_to 'Add to Cart', line_items_path(product_id: product), :class => "btn btn-primary "
        